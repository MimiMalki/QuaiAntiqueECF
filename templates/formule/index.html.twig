{% extends 'base.html.twig' %}

{% block title %}Formule index{% endblock %}

{% block  body %}
    <h1>Formule index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for formule in formules %}
            <tr>
                <td>{{ formule.id }}</td>
                <td>{{ formule.description }}</td>
                <td>{{ formule.price }}</td>
                <td>
                    <a href="{{ path('app_formule_show', {'id': formule.id}) }}">show</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>





{% endblock %}
