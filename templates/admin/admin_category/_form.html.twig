{{ form_start(form) }}
<div>
	{{ form_label(form.label,'Categorie') }}
	{{ form_widget(form.label) }}
</div>
<br>
<div class="d-grid gap-2 col-6 mx-auto">
	<button class="btn btn-dark "><ion-icon name="create"></ion-icon>{{ button_label|default('Ajouter') }}</button>
</div>
{{ form_end(form) }}
