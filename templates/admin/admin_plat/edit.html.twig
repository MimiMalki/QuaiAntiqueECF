{% extends "adminBase.html.twig" %}

{% block title %}
	<title>
		Admin plat |Quai Antique</title>
{% endblock %}
{% block stylesheets %}{% endblock %}
{% block body %}

	{% include "admin/_partials/_cards.html.twig" %}
	<div class="detailss">
		<div class="recentOrders card">
			<div class="cardHeader">
				<h2>
					<strong>Modifier le plat{{ plat.id }}</strong>
				</h2>
				<a href="{{ path('app_admin_plat_index') }}" class="btn">Liste des plats</a>
				<a href="{{ path('app_admin_plat_new') }}" class="btn">
				<ion-icon name="add-outline"></ion-icon>Ajouter un plat</a>
			</div>
			{{ include('admin/admin_plat/_form.html.twig', {'button_label': 'Modifier'}) }}
		{% endblock %}
