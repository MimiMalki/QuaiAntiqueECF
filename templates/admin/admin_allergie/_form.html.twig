{{ form_start(form) }}
<div>
	{{ form_label(form.name,"Le nom de l'allergie") }}
	{{ form_widget(form.name,{'att':{'class':''}}) }}
</div>
<br>
<div class="d-grid gap-2 col-6 mx-auto">
	<button class="btn btn-dark ">
		<ion-icon name="Ajouter"></ion-icon>
		{{ button_label|default('Ajouter') }}</button>
</div>

{{ form_end(form) }}
